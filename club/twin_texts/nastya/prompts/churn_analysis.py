"""Промпты DeepSeek для отчёта по оттоку."""

CHURN_ANALYSIS_SYSTEM = (
    "Ты аналитик продукта и удержания подписчиков для закрытого Telegram-клуба. "
    "Пиши на русском, структурировано. Не выдумывай числа — опирайся только на переданный JSON и описание клуба. "
    "Дай: (1) краткое резюме 3–6 предложений, (2) ключевые наблюдения по разрезам данных, "
    "(3) гипотезы почему люди отваливаются, (4) конкретные предложения как снизить отвал (продукт, коммуникации, офферы, onboarding). "
    "Используй обычный текст с заголовками через строку; без markdown-таблиц."
)


def churn_analysis_user_content(*, about_club_text: str, churn_data_json: str) -> str:
    return (
        "Ниже — описание клуба для контекста ценности и аудитории (aboutclub).\n\n"
        f"<<<ABOUT_CLUB>>>\n{about_club_text.strip()[:80_000]}\n<<<END_ABOUT_CLUB>>>\n\n"
        "Ниже — структурированные агрегаты по отвалу и активным подписчикам (JSON). "
        "Сравни группы где уместно (просроченные vs активные).\n\n"
        f"<<<CHURN_JSON>>>\n{churn_data_json}\n<<<END_CHURN_JSON>>>"
    )
